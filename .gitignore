node_modules
dist
.env
.env.local
.env.*.local
.env.vercel
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json
.idea
coverage
.firebase
firebase-debug.log
.firebaserc.local
.superpowers/
.playwright-mcp/
.vercel

# Client agenda source files (real client docs — kept out of the repo; used
# locally as input for agenda population). Includes Word ~$ lock files.
docs/Existing_Agendas/

# Internal SOP seed content (lives in Firestore, not git)
src/seed/sops/
src/seed/*.local.js
src/seed/tasks/
